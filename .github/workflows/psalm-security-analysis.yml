name: Psalm Security Analysis

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'

jobs:
  psalm:
    uses: opentelemetry-php/gh-workflows/.github/workflows/psalm-security-analysis.yml@main

